# Add greeting utility

## Task

Create a `greet(name: str) -> str` function in `src/maistro/utils/greet.py` that returns "Hello, {name}!".

## Requirements

- Type hints on all parameters and return
- Handle empty string name by returning "Hello, World!"

---

## Error

Needs clarification:
I want to make sure I understand your request correctly.

I interpreted this as: **Writing, fixing, or modifying software code (Python, JavaScript, etc.)**
(confidence: 40%)

Your message: "# Add greeting utility

## Task

Create a `greet(name: str) -> str` function in `src/maistro/utils/greet.py` that returns "Hello, {name}!".

## Requirements

- Type hints on all parameters and return
- "

Is this correct? If not, could you rephrase what you'd like me to do?
For example:

- For coding: "fix the bug in...", "create a function that..."
- For home automation: "tell Abra to turn off the lights"
- For documents: "create a report about..."
