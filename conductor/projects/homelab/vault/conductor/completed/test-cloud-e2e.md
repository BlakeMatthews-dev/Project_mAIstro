# Task: Add a health check endpoint

Add a `/health` endpoint to `src/maistro/api.py` that returns `{"status": "ok", "version": "0.1.0"}`.
This should be a simple GET endpoint using FastAPI.

---

## Result

Task task-831fe872 completed successfully.
