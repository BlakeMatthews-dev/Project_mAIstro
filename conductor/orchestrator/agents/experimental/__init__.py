"""Experimental agent modules — heartbeat features, not in the critical task path.

These modules only run during heartbeat cycles or on explicit trigger.
They are safe to disable without affecting task processing.
"""
